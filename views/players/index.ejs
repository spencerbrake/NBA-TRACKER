<%- include('../partials/header') %>
<body>
  <h1>BALLERS LIST</h1>
  <table id='list'>
      <thead>
          <tr>
              <th>Name</th>
              <th>Rings</th>
              <th>Active</th>
              <th>Details</th>
          </tr>
      </thead>
      <tbody>
          <% players.forEach(function(p) { %>
              <tr>
                  <td><%= p.name %></td>
                  <td><%= p.playerRings %></td>
                  <td><%= p.activePlayer ? 'Yes' : 'Nope' %></td>
                  <td><a href="/players/<%= p._id %>">DETAILS</a></td>
              </tr>
          <% }); %>
      </tbody>
  </table>
</body>
